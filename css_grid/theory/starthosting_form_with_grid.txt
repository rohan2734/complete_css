add display:grid to start hosting form 

and to make the drop down occupy 2 rows instead of  1 

if we give grid-row:2 
it simply means that we have removed it from the dom order, so the next element
in the dom order appears 

so we will remove it and add grid-column : span 2